export interface Service {
  id: number;
  title: string;
  description: string;
  icon: string;
  link: string;
}

export interface WhyChooseUsItem {
  id: number;
  title: string;
  description: string;
  icon: string;
}

export interface ContactInfo {
  name: string;
  title: string;
  address: string;
  email: string;
  phone: string;
  workingHours: string;
}